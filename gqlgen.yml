# schema tells gqlgen where the GraphQL schema is located.
schema:
  - ./**/*.graphql

# resolver reports where the resolver implementations go.
#resolver:
#  layout: follow-schema
#  filename_template: "gql.gen.go"
#  filename_template: "z.custom.go"

# gqlgen will search for any type names in the schema in these go packages
# if they match it will use them, otherwise it will generate them.

# autobind tells gqngen to search for any type names in the GraphQL schema in the
# provided package. If they match it will use them, otherwise it will generate new.
autobind:
  - "project/ent"
  - "project/custom"


# This section declares type mapping between the GraphQL and Go type systems.
models:
  # Defines the ID field as Go 'int'.
  ID:
    model: project/utils/entFields.UUID
  DateOnly:
    model: project/utils/entFields.DateOnly
  TimeOnly:
    model: project/utils/entFields.TimeOnly
  NullString:
    model: project/utils/entFields.NullString
  JSON:
    model: project/utils/entFields.JSON
  Node:
    model:
      - project/ent.Noder

# Where should any generated models go?
model:
  filename: ent/resolvers/models.go
  package: resolvers


# Where should the generated server code go?
exec:
  filename: ent/resolvers/generated.go
  package: resolvers